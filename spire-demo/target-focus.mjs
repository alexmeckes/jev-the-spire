// Grounded in visible opposing-facing rules; never select a target by boss name.
export function targetFocusReview(state) {
 const enemies=(state.battle?.enemies??[]).filter(e=>e.hp>0);
 if(enemies.length!==2 || !enemies.every(e=>(e.status??[]).some(p=>/more damage.*from behind/i.test(p.description??''))))return '';
 return 'PRIMARY DAMAGE TARGET: Choose one primary enemy to finish, comparing remaining HP, block, visible defenses, incoming pressure and ally-death effects. Concentrate the bulk of damage on that enemy across turns rather than leaving both alive with similar partial damage. Use recent observations to retain that commitment; a cheap final action used to turn does not redefine the primary damage target. Separate damage focus from end-of-turn facing: reserve the cheapest useful legal targeted card or potion to face the dangerous attacker when needed, while placing larger attacks on the primary target. Compare safe focused lines against splitting damage. Change the primary target only for a concrete advantage such as an immediate kill, necessary damage prevention or a visible mechanic; do not switch merely because the other enemy attacks this turn. Read ally-death Strength and Block effects before finishing one enemy: a kill can make the survivor dangerous, so compare the resulting survival and re-observe after the kill. Do not assume death removes all incoming damage or guarantees victory. Keep every supplied target and candidate available; Jev chooses the tradeoff.';
}
